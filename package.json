{
  "name": "kuccps-cluster-system",
  "private": true,
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build && npm --prefix backend-server ci && npm --prefix backend-server run build",
    "start": "npm --prefix backend-server run start"
  }
}
